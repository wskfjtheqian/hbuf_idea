create table ${util.camelToUnderline(${dataName})}(
#foreach ($field in $fields)
    ${field.name} ${field.sqlType} ${field.defaultValue} comment '${field.comment}',
#end
    delete_time timestamp(3)         null comment '删除时间',
    is_deleted  tinyint(1) default 0 not null comment '是否删除',
    primary key (id)
) comment '${comment}';
